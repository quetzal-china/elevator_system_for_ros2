# 题目二-Action与电梯调度系统
> Author: liuyuxin
---

## 一、需求分析与实现思路

### 总体目标
使用ROS2 Action通信机制实现一个电梯调度系统，满足三个任务点的要求。

### 任务点一: 基础Action通信理解与应用
要求:
- 模拟一次电梯接送客过程(1-10层)
- 电梯初始状态在1楼
- 获取乘客当前楼层和目标楼层信息
- 利用Action通信特性实现实时反馈
- 电梯运行速度: 0.5s/楼
- 开关门时间: 1s
- 停留时间: 0s
### 任务点二: 多次接送客过程
要求:
- 模拟8次电梯接送客过程
- 乘客初始楼层和目标楼层用随机数生成
- 数据合理性检查
- 运行过程中会间隔收到请求
### 任务点三: 方向导向的接送客过程
要求:
- 同样模拟8次电梯接送客过程
- 初始信息只有乘客当前楼层和行进方向(UP/DOWN)
- 接到乘客后才能获得目标楼层
- 数据合理性检查(如不能出现5楼上行到2楼的情况)
  
### 技术实现思路
#### 1. Action接口设计
需要定义一个适合电梯调度的Action接口，包含:
- Goal: 乘客请求信息
- Result: 任务完成结果
- Feedback: 执行过程中的状态反馈
#### 2. 服务器端实现
需要实现电梯Action服务器，具备以下功能:
- 接收和处理电梯请求
- 实现电梯移动逻辑(上行/下行)
- 实时反馈电梯状态
- 处理开关门操作
- 支持任务取消功能
#### 3. 客户端实现
需要实现电梯Action客户端，具备以下功能:
- 发送电梯请求
- 接收反馈信息并显示
- 处理最终结果

### 三个任务点的具体实现方案
#### 任务点一实现方案
- 创建简单的客户端发送一次固定请求
- 服务器处理单次接送任务
- 展示完整的Action通信流程
#### 任务点二实现方案
- 客户端生成8个随机的楼层请求
- 服务器按顺序处理这些请求
- 实现基本的队列处理机制
#### 任务点三实现方案
- 修改Action接口以支持方向信息
- 客户端先发送楼层和方向，接到乘客后再更新目标楼层
- 服务器实现更复杂的调度逻辑
  
### 核心技术要点
#### 1. Action通信机制
- 理解Goal、Result、Feedback的作用
- 掌握服务器端和客户端的实现方法
- 处理异步通信和回调机制
#### 2. 电梯调度算法
- 电梯状态管理(当前楼层、运行方向、载客状态)
- 楼层移动模拟(时间控制)
- 开关门操作模拟
#### 3. 并发处理
- 多请求处理机制
- 队列管理
- 调度策略实现
### 文件结构规划
elevator_action/
├── action/
│   └── Elevator.action      # Action接口定义
├── elevator_action/
│   ├── __init__.py
│   ├── elevator_server.py   # 服务器端实现
│   └── elevator_client.py   # 客户端实现
├── package.xml              # 包依赖配置
├── setup.py                 # Python包配置
└── CMakeLists.txt           # 构建配置

---
## 二、代码实现

### 1. Action接口定义
**Goal定义**: 客户端告诉服务端要做什么
在这个情景下,Goal包含以下信息:
- 乘客当前楼层(起始楼层)
- 乘客目标楼层
- 电梯运行方向(UP/DOWN)
代码体现为
```action
# Goal - 乘客请求电梯的参数
int32 initial_floor  # 起始楼层
int32 target_floor   # 目标楼层
string directiontogo # 电梯运行方向(UP/DOWN)
```
**Result定义**: 服务端向客户端,任务完成后的最终输出
在这个情景下,Result包含以下信息:
- 是否成功完成
- 最终所在楼层
- 完成信息
代码体现为
```action
# Result  - 任务完成后的结果
bool success         # 是否成功完成
int32 final_floor    # 最终所在楼层
string message       # 完成信息
```
**Feedback定义**: 服务端向客户端实时汇报任务执行进度
在这个情景下,Feedback包含以下信息:
- 当前楼层
- 当前状态(移动中、开关门等)
- 乘客数量
- 电梯运行方向(UP/DOWN)
代码体现为
```action
# Feedback  - 执行过程中的反馈信息
int32 current_floor  # 当前楼层
string status        # 当前状态(MOVING UP/MOVING DOWN/ARRIVED/PICKUP DOWN/DROPOFF DONE)
string direction     # 电梯运行方向(UP/DOWN)
```

### 2. BUILD
在终端中执行以下命令,编译Action接口:
```bash
cd ~/ros2_ws
colcon build --packages-select elevator_action
```
终端输出:
```bash
liuyuxin@ubuntu:~/ros2_ws$ colcon build --packages-select elevator_system
Starting >>> elevator_system
--- stderr: elevator_system                         
CMake Error at CMakeLists.txt:21 (find_package):
  By not providing "Findrclpy_action.cmake" in CMAKE_MODULE_PATH this project
  has asked CMake to find a package configuration file provided by
  "rclpy_action", but CMake did not find one.

  Could not find a package configuration file provided by "rclpy_action" with
  any of the following names:

    rclpy_actionConfig.cmake
    rclpy_action-config.cmake

  Add the installation prefix of "rclpy_action" to CMAKE_PREFIX_PATH or set
  "rclpy_action_DIR" to a directory containing one of the above files.  If
  "rclpy_action" provides a separate development package or SDK, be sure it
  has been installed.


make: *** [Makefile:748：cmake_check_build_system] 错误 1
---
Failed   <<< elevator_system [0.30s, exited with code 2]

Summary: 0 packages finished [0.49s]
  1 package failed: elevator_system
  1 package had stderr output: elevator_system
liuyuxin@ubuntu:~/ros2_ws$ 
```
问题在于：
- 使用的是 C++ 代码（ament_cmake构建系统）
- 但在package.xml和CMakeLists.txt中使用的是Python版本的Action库 rclpy_action
- C++应该使用 rclcpp_action

---

cd ~/ros2_ws
colcon build --packages-select elevator_system

ls -la ~/ros2_ws/src/elevator_system/

ls -la ~/ros2_ws/src/elevator_system/action/
